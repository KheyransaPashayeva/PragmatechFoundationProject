# Task
# The provided code stub reads and integer, n, from STDIN. For all non-negative integers i<n, print i^2(kvadrat) .
# The first and only line contains the integer,n.
# Constraints   / n=[1,20]
# Output Format
# Print n lines, one corresponding to each i.
n = int(input('eded daxil edin'))
if 1<= n <= 20:
    for i in range(0,n):
       print(i*i)
